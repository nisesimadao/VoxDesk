# 画面。OS 本物の部品を使う（Windows なら Win32 のコントロールがそのまま出る）
wxPython>=4.2
# 音声の入出力とリアルタイム処理
sounddevice>=0.5
numpy>=1.26
scipy>=1.11
pedalboard>=0.9        # ノイズゲート/コンプ/リバーブ/VST3 ホスト
# 動画の再生（FFmpeg 同梱。外部インストール不要）
av>=13
pillow>=10
# 曲の検索と音源の取得
yt-dlp>=2024.1
# Windows のデバイス状態（ミュート・レベル）の確認。無くても動く
pycaw>=20240210
comtypes>=1.2
